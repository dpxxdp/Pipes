
//bring in cache and mongo

function GetNext(params, callback) {
    
    var low_i = param[0];
    var high_i = param[1];
    
    //cache query for those rows
    //else mongo query for those rows
    //if we get an error
    //callback(error)
    //if we get good data
    //callback(null, customerDataBatch)    
    
}


function GetAll(params, callback) {
    
    var options = params;
    
    //cache query for those rows
    //else mongo query for those rows
    //if we get an error
    //callback(error)
    //if we get good data
    //callback(null, customerDataBatch)    
    
}


function Push(params, callback) {
    
    var low_i = param[0];
    var high_i = param[1];
    
    //cache query for those rows
    //else mongo query for those rows
    //if we get an error
    //callback(error)
    //if we get good data
    //callback(null)    
    
}