

function retrieveWithParams(params, callback) {
    
    
    //retrieve data given params
    //if retrieval fails
        //return callback(error)
    //if retrieval succeeds
        //responseAsJSON = data; for this, google "JSON rpc response", first link, 5 Response Object
        //callback(null, responseAsJSON);
    
    
    
}